package com.fincity.nocode.kirun.engine.model;

public enum StatementType {

	ASSERT, BREAK, CATCH, CASE, CONTINUE, END, ELSE, ELSEIF, EXPRESSION, FINALLY, FUNCTION, IF, LOOP, START, SWITCH,
	THEN, THROW, TRY, PARALLELIZE, MERGE,;
}
