export class DateCompareUtil {
    private YEAR = 1;

    private MONTH = 2;

    private DAY = 3;

    private HOUR = 4;

    private MINUTE = 5;

    private SECOND = 6;

    public static compare(
        firstDate: string,
        secondDate: string,
        operationName: string,
        fields: [],
    ): boolean {
        for (let i = 0; i < fields.length; i++) {}

        return true;
    }

    private compareFields(
        firstDate: string,
        secondDate: string,
        operationName: string,
        fieldName: string,
    ) {
        if (fieldName == "year") {

        } 
    }

    private checkFields(firstDate: string, secondDate: string, operationName: string, ) {

    }
}
